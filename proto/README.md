# (empty — pre-release)

No contract has been published yet. `publishPartnerProtoContracts.bash` populates
`proto/travel/wink/grpc/v1/*.proto` here on the first successful production release. This
placeholder file is removed by that first sync.
